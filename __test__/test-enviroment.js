require('dotenv').config()

export const config = {
    URL: process.env.URL,
    CREDENTIAL_TYPE: process.env.CREDENTIAL_TYPE,
    CREDENTIAL_FILE_TYPE: process.env.CREDENTIAL_FILE_TYPE,
    CREDENTIAL_FILE_PROYECT_ID: process.env.CREDENTIAL_FILE_PROYECT_ID,
    CREDENTIAL_FILE_PRIVATE_KEY_ID: process.env.CREDENTIAL_FILE_PRIVATE_KEY_ID,
    CREDENTIAL_FILE_PRIVATE_KEY: process.env.CREDENTIAL_FILE_PRIVATE_KEY,
    CREDENTIAL_FILE_CLIENT_EMAIL: process.env.CREDENTIAL_FILE_CLIENT_EMAIL,
    CREDENTIAL_FILE_CLIENT_ID: process.env.CREDENTIAL_FILE_CLIENT_ID,
    CREDENTIAL_FILE_AUTH_URI: process.env.CREDENTIAL_FILE_AUTH_URI,
    CREDENTIAL_FILE_TOKEN_URI: process.env.CREDENTIAL_FILE_TOKEN_URI,
    CREDENTIAL_FILE_AUTH_PROV_CERT: process.env.CREDENTIAL_FILE_AUTH_PROV_CERT,
    CREDENTIAL_FILE_CLIENT_CERT: process.env.CREDENTIAL_FILE_CLIENT_CERT,
    REALTIME_ROUTE: process.env.REALTIME_ROUTE,
    FIRESTORE_ROUTE: process.env.FIRESTORE_ROUTE,
}